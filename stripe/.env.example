# Stripe Driver - Environment Configuration Example
#
# Copy this file to .env and fill in your actual values
# NEVER commit your .env file with real credentials!
#
# Usage:
#   1. Copy this file: cp .env.example .env
#   2. Edit .env with your real credentials
#   3. Load in Python:
#      from dotenv import load_dotenv
#      load_dotenv()

# Required: Stripe API Key
# Get your API key from: https://dashboard.stripe.com/apikeys
#
# For testing: Use a key starting with sk_test_
# For production: Use a key starting with sk_live_
#
# IMPORTANT: Never commit real API keys to version control!
STRIPE_API_KEY=sk_test_your_api_key_here

# Optional: Stripe API Base URL
# Default: https://api.stripe.com
# Only change this if using a different Stripe endpoint
STRIPE_BASE_URL=https://api.stripe.com

# Optional: Debug mode
# Set to "true" to enable debug logging for API calls
# WARNING: Debug mode logs request/response details (may include sensitive data)
# NEVER enable in production!
DEBUG=false

# Optional: Request timeout in seconds
# Default: 30
# Increase if you're getting timeout errors
TIMEOUT=30

# Optional: Maximum retry attempts for rate limiting
# Default: 3
# Increase for higher-volume operations
MAX_RETRIES=3
