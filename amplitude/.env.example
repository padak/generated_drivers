# Amplitude Analytics Driver - Environment Configuration
#
# This file demonstrates required and optional environment variables.
# Copy this to .env and fill in your actual values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# It should only contain secrets and be kept locally.

# ============================================================================
# REQUIRED
# ============================================================================

# Amplitude API Key
# Get this from: Amplitude Dashboard → Settings → API Keys → Projects
# Must be at least 20 characters
AMPLITUDE_API_KEY=your_amplitude_api_key_here

# ============================================================================
# OPTIONAL
# ============================================================================

# API Base URL (defaults to standard US endpoint)
# Standard US: https://api2.amplitude.com
# EU: https://api.eu.amplitude.com
# AMPLITUDE_BASE_URL=https://api2.amplitude.com

# Request timeout in seconds (default: 30)
# Increase for slow networks or large payloads
# AMPLITUDE_TIMEOUT=30

# Maximum retry attempts on rate limit (default: 3)
# Increase for high-volume or unreliable networks
# AMPLITUDE_MAX_RETRIES=3

# Enable debug logging (default: false)
# Shows all API calls, retries, and errors
# Set to "true" to enable
# AMPLITUDE_DEBUG=false

# ============================================================================
# DEVELOPMENT SETTINGS (optional)
# ============================================================================

# Log level (default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# Environment name (for monitoring/logging)
# ENVIRONMENT=development

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. Get your API key:
#    - Log in to Amplitude Dashboard
#    - Go to Settings → Developers → API Keys
#    - Find your "Project API Key"
#
# 2. Create local .env file:
#    cp .env.example .env
#
# 3. Edit .env and set AMPLITUDE_API_KEY:
#    AMPLITUDE_API_KEY=your_actual_key_here
#
# 4. Load in your Python code:
#    from dotenv import load_dotenv  # pip install python-dotenv
#    import os
#    load_dotenv()
#    api_key = os.getenv("AMPLITUDE_API_KEY")
#
# 5. Or use with AmplitudeDriver:
#    from amplitude import AmplitudeDriver
#    driver = AmplitudeDriver.from_env()  # Reads AMPLITUDE_API_KEY
#
# ============================================================================
# TROUBLESHOOTING
# ============================================================================
#
# Q: "AuthenticationError: Missing Amplitude API key"
# A: Make sure AMPLITUDE_API_KEY is set in your .env file
#    and you've called: load_dotenv()  # or export AMPLITUDE_API_KEY=...
#
# Q: "ConnectionError: Cannot reach API"
# A: Verify AMPLITUDE_BASE_URL is correct for your region (US or EU)
#    Check your internet connection
#
# Q: "RateLimitError: Rate limit exceeded"
# A: Increase AMPLITUDE_MAX_RETRIES or add delays between requests
#
# Q: How do I get my API key?
# A: Amplitude Dashboard → Settings → Developers → API Keys
#
# ============================================================================
