# Odoo Driver - Environment Variables Template
# =============================================
#
# This is a template file showing all environment variables used by the Odoo driver.
#
# IMPORTANT: This file contains NO real credentials!
# Copy this to .env and fill in your actual values.
#
# WARNING: Never commit .env file to version control!
# Always use .env.example as a template instead.

# Core Configuration (REQUIRED)
# =============================

# Odoo Instance URL
# The full URL to your Odoo instance
# Example: https://odoo.mycompany.com
# Example: https://odoo.example.com:8069
ODOO_BASE_URL=https://your-instance.odoo.com

# Database Name
# The name of the Odoo database to connect to
# Example: production
# Example: staging
ODOO_DATABASE=your_database_name

# API Key
# User's API key for authentication
# Generate this in Odoo: Settings → Users → Your User → Security → New API Key
# WARNING: Keep this secret! Never commit to version control!
ODOO_API_KEY=your_user_api_key_here


# Optional Configuration
# ======================

# Request Timeout (seconds)
# How long to wait for API responses before timing out
# Default: 30
# Increase if your Odoo instance is slow
# ODOO_TIMEOUT=30

# Maximum Retries
# Number of times to retry failed requests (rate limiting, temporary errors)
# Default: 3
# Increase for unstable connections
# ODOO_MAX_RETRIES=3

# Debug Mode
# Enable detailed logging of API calls and responses
# Default: false
# Set to 'true' or '1' to enable
# ODOO_DEBUG=false


# Setup Instructions
# ==================
#
# 1. Copy this file to .env in the same directory:
#    cp .env.example .env
#
# 2. Edit .env with your actual credentials:
#    ODOO_BASE_URL=https://your-actual-instance.odoo.com
#    ODOO_DATABASE=your_actual_database
#    ODOO_API_KEY=your_actual_api_key
#
# 3. Verify .env is in .gitignore (it should be):
#    echo ".env" >> .gitignore
#
# 4. Use in your application:
#    from odoo_driver import OdooDriver
#    client = OdooDriver.from_env()
#
# 5. Never commit .env to version control!


# Security Best Practices
# =======================
#
# ✓ Keep ODOO_API_KEY secret - treat it like a password
# ✓ Rotate API keys periodically
# ✓ Use different keys for different environments (dev/staging/prod)
# ✓ Store .env file securely with restricted file permissions
# ✓ Use a secrets management system in production
# ✓ Never log or print the API key
# ✓ Review .gitignore to ensure .env is excluded
#
# On Unix/Linux systems, restrict file permissions:
#   chmod 600 .env
#
# On Windows systems, restrict using file properties or:
#   icacls .env /inheritance:r /grant:r "%username%:F"


# Testing & Development
# =====================
#
# For testing, you might want to use a test/staging Odoo instance:
#
#   ODOO_BASE_URL=https://test-odoo.mycompany.com
#   ODOO_DATABASE=test_database
#   ODOO_API_KEY=test_api_key
#
# Never use production credentials in development!


# Troubleshooting
# ===============
#
# If you get "Missing required environment variables" error:
# 1. Verify all REQUIRED variables are set (ODOO_BASE_URL, ODOO_DATABASE, ODOO_API_KEY)
# 2. Check variable names are exactly correct (case-sensitive on Linux/Mac)
# 3. Ensure .env file is in the correct directory
# 4. Verify the .env file format (no extra spaces around =)
#
# If you get "Authentication Error":
# 1. Verify ODOO_API_KEY is correct
# 2. Check the API key hasn't expired
# 3. Verify the user has API access permission in Odoo
#
# If you get "Connection Error":
# 1. Verify ODOO_BASE_URL is correct and reachable
# 2. Check if Odoo instance is running
# 3. Verify network connectivity to the instance
#
# For more help, see README.md in the same directory.
