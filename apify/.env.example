# Apify Driver - Environment Variables Template
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# Apify API Token
# Get your token from: https://console.apify.com/settings/integrations
# This is REQUIRED for authentication
APIFY_API_TOKEN=your_token_here

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Apify API Base URL
# Default: https://api.apify.com/v2
# Only change if using a custom Apify deployment or proxy
# APIFY_API_URL=https://api.apify.com/v2

# ============================================================================
# DEVELOPMENT CONFIGURATION (Optional)
# ============================================================================

# Enable debug logging
# Set to "true" to see all API calls logged
# Default: false (disabled)
# DEBUG=false

# Request timeout in seconds
# Default: 30 seconds
# Increase for slow networks or large responses
# REQUEST_TIMEOUT=30

# Maximum retry attempts on rate limit (HTTP 429)
# Default: 3 retries with exponential backoff
# Set to 0 for no retries, 1 for single retry, etc.
# MAX_RETRIES=3

# ============================================================================
# NOTES
# ============================================================================

# 1. SECURITY
#    - Never commit .env with real credentials to git
#    - Add .env to .gitignore (already done)
#    - Use .env.example as template for documentation
#    - Only commit .env.example (no credentials)

# 2. ENVIRONMENT VARIABLES
#    These will be loaded from:
#    a) .env file (if exists)
#    b) shell environment variables
#    c) system environment variables

# 3. LOADING CREDENTIALS
#    from apify_driver import ApifyDriver
#
#    # Automatically loads from environment
#    client = ApifyDriver.from_env()
#
#    # Or manually set
#    client = ApifyDriver(
#        api_url="https://api.apify.com/v2",
#        api_key="your_token_here"
#    )

# 4. TOKEN SECURITY
#    - Create tokens at: https://console.apify.com/settings/integrations
#    - Use minimal required permissions
#    - Rotate tokens regularly
#    - Revoke unused tokens immediately
#    - Never share tokens via email or chat
